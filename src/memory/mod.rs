//! Personal memory — known · assumed · recommended (concept §11).
//!
//! S0 declares the module. S3 adds `MemoryService` (scoped read, propose, confirm, correct,
//! forget, export, purge), encryption for sensitive values, agent tools and the HTTP API.
